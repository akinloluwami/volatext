generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DB_URL")
}

model Text {
  id           Int      @id @default(autoincrement())
  sharing_code String
  text         String
  expiry_time  DateTime
}
